# FlashScale Agent Guidelines

## Communication

- 작업 설명과 회고는 한국어로 작성한다.
- 큰 변경을 시작하기 전에 변경 범위와 영향을 설명한다.
- 요구사항이 불명확하면 임의로 기능을 확대하지 않는다.
- 한 번에 하나의 task 문서만 수행한다.
- 본 사용자에게 과외를 한다고 생각하고 작은 부분도 근거를 설명한다.

## Project Context

- 작업을 시작하기 전에 `docs/project-charter.md`와 현재 활성 task 문서를 읽는다.
- 구현 결정은 프로젝트 차터의 목표와 v1 범위를 따라야 한다.
- task와 프로젝트 차터가 충돌하면 작업을 중단하고 사용자에게 알린다.

## Scope

- 현재 작업은 docs/tasks/ 아래의 활성 task를 기준으로 한다.
- v1 제외 기능을 임의로 추가하지 않는다.
- Redis, Kafka, 로그인, 결제, 프론트엔드를 추가하지 않는다.
- 새로운 프레임워크나 인프라 도구를 추가할 때는 이유와 대안을 기록한다.

## Architecture

- ticketing-api는 Spring Boot 애플리케이션이다.
- predictor는 Python 기반 예측 컴포넌트다.
- 두 애플리케이션은 독립적으로 실행·배포할 수 있어야 한다.
- Controller는 요청 검증과 응답 변환만 담당한다.
- 비즈니스 로직은 Service/Application 계층에 둔다.
- JPA Entity를 API 응답으로 직접 노출하지 않는다.
- 동시성 안전성을 기능적 정확성의 일부로 취급한다.

## Verification

- 변경 후 관련 테스트를 실행한다.
- Spring 변경 시 최소 `./gradlew test`를 실행한다.
- 완료 전 전체 검증 스크립트 `./scripts/verify.sh`를 실행한다.
- 테스트를 실행하지 못했다면 그 이유를 명시한다.
- 테스트 실패를 삭제하거나 우회해서 작업을 완료하지 않는다.

## Change Policy

- 요청과 관계없는 리팩터링을 하지 않는다.
- 기존 사용자 변경사항을 덮어쓰지 않는다.
- 비밀값, 인증정보, `.env` 파일을 커밋하지 않는다.
- 생성 파일과 빌드 산출물을 직접 수정하지 않는다.
- 새로운 의존성은 꼭 필요한 경우에만 추가한다.

## Definition of Done

- task의 acceptance criteria가 충족되었다.
- 테스트와 검증 명령이 통과했다.
- 문서가 실제 동작과 일치한다.
- 남은 위험이나 후속 작업이 기록되었다.

## ETC
- 완료된 작업은 항상 체크리스트에 체크 표시를 수행한다.
